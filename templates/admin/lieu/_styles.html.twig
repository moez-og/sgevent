<style>
    .lieu-feedback {
        border-radius: 14px;
        padding: 12px 14px;
        margin-bottom: 12px;
        border: 1px solid transparent;
        font-weight: 600;
    }

    .lieu-feedback--success {
        border-color: #86efac;
        background: #f0fdf4;
        color: #166534;
    }

    .lieu-feedback--error {
        border-color: #fca5a5;
        background: #fef2f2;
        color: #991b1b;
    }

    .lieu-card,
    .lieu-form-card,
    .lieu-detail-card,
    .lieu-delete-card {
        background: #fff;
        border: 1px solid #e5e7eb;
        border-radius: 18px;
        padding: 20px;
        box-shadow: 0 12px 30px rgba(15, 23, 42, 0.04);
    }

    .lieu-toolbar,
    .lieu-actions,
    .lieu-page-actions {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        align-items: center;
    }

    .lieu-search-card {
        background: #fff;
        border: 1px solid #e5e7eb;
        border-radius: 18px;
        padding: 18px;
        margin-bottom: 18px;
        box-shadow: 0 12px 30px rgba(15, 23, 42, 0.04);
    }

    .lieu-search-actions {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        align-items: center;
        margin-top: 12px;
    }

    .lieu-sort-link {
        color: inherit;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 6px;
    }

    .lieu-sort-link:hover {
        color: #2563eb;
    }

    .lieu-pagination {
        display: flex;
        justify-content: center;
        margin-top: 20px;
    }

    .lieu-empty-state {
        text-align: center;
        padding: 28px;
        color: #475569;
        background: #f8fafc;
        border: 1px dashed #cbd5e1;
        border-radius: 18px;
    }

    .lieu-image-preview {
        background: #f8fafc;
        border: 1px solid #e2e8f0;
        border-radius: 16px;
        padding: 12px;
    }

    .lieu-gallery {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
        gap: 12px;
    }

    .lieu-gallery img {
        width: 100%;
        height: 140px;
        object-fit: cover;
        border-radius: 14px;
        border: 1px solid #e2e8f0;
    }

    .lieu-table {
        width: 100%;
        border-collapse: collapse;
        overflow: hidden;
    }

    .lieu-table th,
    .lieu-table td {
        padding: 12px 10px;
        border-bottom: 1px solid #e5e7eb;
        text-align: left;
        vertical-align: top;
        font-size: 14px;
    }

    .lieu-table th {
        background: #f8fafc;
        color: #334155;
        font-weight: 700;
    }

    .lieu-badge {
        display: inline-flex;
        align-items: center;
        border-radius: 999px;
        padding: 6px 10px;
        font-size: 12px;
        font-weight: 700;
        background: #eff6ff;
        color: #1d4ed8;
    }

    .lieu-badge--neutral {
        background: #f8fafc;
        color: #475569;
    }

    .lieu-badge--success {
        background: #ecfdf5;
        color: #047857;
    }

    .lieu-meta-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 12px;
        margin-top: 18px;
    }

    .lieu-meta {
        border: 1px solid #e2e8f0;
        border-radius: 14px;
        padding: 14px;
        background: #f8fafc;
    }

    .lieu-meta__label {
        font-size: 12px;
        font-weight: 700;
        color: #64748b;
        text-transform: uppercase;
        letter-spacing: .04em;
        margin-bottom: 6px;
    }

    .lieu-meta__value {
        color: #0f172a;
        font-weight: 600;
        word-break: break-word;
    }

    .lieu-form-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 14px;
    }

    .lieu-field--full {
        grid-column: 1 / -1;
    }

    .lieu-form-card label {
        font-size: 13px;
        font-weight: 700;
        color: #334155;
        margin-bottom: 6px;
        display: inline-block;
    }

    .lieu-form-card input,
    .lieu-form-card select,
    .lieu-form-card textarea {
        width: 100%;
        border: 1px solid #cbd5e1;
        border-radius: 12px;
        padding: 10px 12px;
        font-size: 14px;
        background: #fff;
        color: #0f172a;
        outline: none;
    }

    .lieu-form-card input:focus,
    .lieu-form-card select:focus,
    .lieu-form-card textarea:focus {
        border-color: #38bdf8;
        box-shadow: 0 0 0 3px rgba(56, 189, 248, 0.15);
    }

    .lieu-form-card ul {
        margin: 8px 0 0;
        padding-left: 18px;
        color: #b91c1c;
        font-size: 13px;
    }

    .lieu-horaires-card {
        border: 1px solid #dbeafe;
        border-radius: 16px;
        background: #f8fbff;
        padding: 14px;
    }

    .lieu-horaires-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 10px;
    }

    .lieu-horaire-day {
        border: 1px solid #dbe3f0;
        border-radius: 12px;
        background: #fff;
        padding: 10px;
    }

    .lieu-horaire-day__head {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        margin-bottom: 8px;
    }

    .lieu-horaire-toggle {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        font-weight: 600;
        color: #334155;
        margin-bottom: 0;
    }

    .lieu-horaire-slot-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 8px;
    }

    .primary-btn {
        border: 0;
        border-radius: 12px;
        background: linear-gradient(135deg, #0ea5e9, #2563eb);
        color: #fff;
        font-weight: 700;
        padding: 11px 18px;
        cursor: pointer;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        justify-content: center;
    }

    .danger-btn {
        border: 0;
        border-radius: 12px;
        background: linear-gradient(135deg, #ef4444, #b91c1c);
        color: #fff;
        font-weight: 700;
        padding: 11px 18px;
        cursor: pointer;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        justify-content: center;
    }

    .secondary-btn {
        border: 1px solid #cbd5e1;
        border-radius: 12px;
        background: #fff;
        color: #0f172a;
        font-weight: 700;
        padding: 11px 18px;
        cursor: pointer;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        justify-content: center;
    }

    .lieu-cover {
        border-radius: 18px;
        background: linear-gradient(135deg, #0f172a, #1d4ed8);
        color: #fff;
        padding: 22px;
        margin-bottom: 18px;
    }

    .lieu-cover__title {
        margin: 0;
        font-size: 28px;
        font-weight: 800;
    }

    .lieu-cover__subtitle {
        margin-top: 8px;
        opacity: .85;
    }

    @media (max-width: 980px) {
        .lieu-form-grid,
        .lieu-meta-grid {
            grid-template-columns: 1fr;
        }

        .lieu-horaires-grid,
        .lieu-horaire-slot-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
